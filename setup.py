from setuptools import setup

setup(
    name="gym_xarm",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'stable_baselines3',
                      'numpy',
                      'matplotlib',
                      'torch']
)
